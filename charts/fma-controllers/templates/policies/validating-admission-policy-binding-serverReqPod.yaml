# This file has been modified with the assistance of Claude Opus 4.6
{{- if .Values.global.enableValidationPolicy }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: bind-fma-bound-serverreqpod
spec:
  policyName: fma-bound-serverreqpod
  matchResources:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["pods"]
        operations: ["UPDATE"]
  validationActions:
    - Deny
{{- end }}
